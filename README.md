
````md
# Database-Connection-Project

This is a simple React project that demonstrates how to connect a frontend (React + Vite) with a backend API (Node.js + Express + MySQL / MAMP).  
The project includes basic CRUD operations such as adding, updating, and deleting data.

---

## 📌 How I Pushed This Project to GitHub

Below are the exact steps I followed to upload this project to my GitHub repository **Database-connection-project** under my account **Naol724**.

### 1. Initialize Git in the project
```bash
git init
````

### 2. Add all project files to Git

```bash
git add .
```

### 3. Make the first commit

```bash
git commit -m "Initial project upload"
```

### 4. Connect the local project to GitHub

```bash
git remote add origin https://github.com/Naol724/Database-connection-project.git
```

### 5. Push the project to GitHub

```bash
git branch -M main
git push -u origin main
```

---

## 🛠️ Technologies Used

* React (Vite)
* JavaScript (ES6+)
* Node.js
* Express.js
* MySQL Database (via MAMP or XAMPP)
* Axios for API requests

---

## 📂 Project Features

* Database connection from backend
* Fetch all data (GET)
* Add new data (POST)
* Update existing data (PUT)
* Delete data (DELETE)
* Simple UI with modern design

---
